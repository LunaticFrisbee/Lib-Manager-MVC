<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
        <link rel="stylesheet" href="/assets/styles/styles.css">
        <title>Checkout List</title>  
    </head>
    <body>
        <div class="complete-wrapper">
            <div class="container text-center">
                <button id="top-btn" class="btn btn-warning" onclick="window.location='/dashboard';">Back to Dashboard</button>
                <br><br>
                <table class="table table-striped">
                    <tr>
                        <th scope="col">Book ISBN</th>
                        <th scope="col">Book Name</th>
                        <th scope="col">Hand In</th>
                    </tr>
                    {% if requestData != NULL %}
                        {% for request in requestData %}
                            <tr>
                                <td>{{request.isbn}}</td>
                                <td>{{request.book}}</td>
                                <td>
                                    <form action="/dashboard/return" method="POST">
                                    <input type="text" name="bookID" value="{{request.isbn}}" style="display:none">
                                    <button class="btn btn-outline-success" type="submit">Hand In</button>
                                    </form>
                                </td>
                            </tr>
                        {% endfor %}
                    {% endif %}
                </table>
            </div>
        </div>
        
    </body>
    <!-- JavaScript Bundle with Popper -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa" crossorigin="anonymous"></script>
</html>