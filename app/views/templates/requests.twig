<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
    <link rel="stylesheet" href="/assets/styles/styles.css">
    <title>Requests</title>
  </head>
  <body>
    <div class="complete-wrapper">
      <div class="container text-center">
        <h3>Pending Requests</h3>
        <table class="table table-striped">
          <tr>
            <th scope="col">ISBN</th>
            <th scope="col">Book Name</th>
            <th scope="col">Requested by</th>
            <th scope="col">Approve</th>
            <th scope="col">Deny</th>
          </tr>
          {% if bookData != NULL %}
              {% for request in bookData %}
                  <tr>
                      <td>{{request.isbn}}</td>
                      <td>{{request.book}}</td>
                      <td>{{request.enrollmentNo}}</td>
                      <td>
                          <form action="/admindashboard/requests/approve" method="POST">
                              <input type="text" name="bookID" value="{{request.isbn}}" style="display: none;"/>
                              <input type="text" name="bookTitle" value="{{request.book}}" style="display: none;"/>
                              <input type="text" name="enrollmentNo" value="{{request.enrollmentNo}}" style="display: none;"/>
                              <button class="btn btn-outline-success" type="submit">Approve</button>
                          </form>
                      </td>
                      <td>
                          <form action="/admindashboard/requests/deny" method="POST">
                                  <input type="text" name="bookID" value="{{request.isbn}}" style="display: none;"/>
                                  <input type="text" name="bookTitle" value="{{request.book}}" style="display: none;"/>
                                  <input type="text" name="enrollmentNo" value="{{request.enrollmentNo}}" style="display: none;"/>
                                  <button class="btn btn-outline-danger" type="submit">Deny</button>
                          </form>
                      </td>
                    </tr>
                  {% endfor %}
                {% endif %}
        </table>
        <br>
        <button class="btn btn-primary" onclick="window.location='/admindashboard'">Go Back to Dashboard</button>
      </div>
    </div>
  </body>
  <!-- JavaScript Bundle with Popper -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa" crossorigin="anonymous"></script>
</html>